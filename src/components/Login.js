import React from 'react';
import TelegramLogin from './TelegramLogin';

const Login = () => {
    return (
        <div>
            <h1>Login Page</h1>
            <TelegramLogin />
        </div>
    );
};

export default Login;